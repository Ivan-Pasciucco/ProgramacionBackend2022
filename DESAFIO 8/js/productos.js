const faker = require('faker');
faker.locale = 'es';

class Productos {

    constructor({
    })

    randomProducts() {
        let producto = {};
        const productos = [];
        for (let index = 0; index < 5; index++) {

        }
    }
}

module.exports = Productos;